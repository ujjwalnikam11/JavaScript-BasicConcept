//Homogeneous array
const numbers = [1, 2, 3, 4, 5]; // All elements are numbers
// Heterogeneous array
const mixedArray = [1, "two", true, null]; // Elements are of different types
console.log(numbers);
console.log(mixedArray);